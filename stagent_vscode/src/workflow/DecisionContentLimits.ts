/** Soft cap for decision record length in webview quality checklist (aligns with prompt §800 字). */
export const DECISION_RECORD_SOFT_MAX_CHARS = 800;

/** LLM 输入上下文压缩摘要目标字数（summarizeForInput prompt）。 */
export const INPUT_CONTEXT_SUMMARY_TARGET_MIN_CHARS = 200;
export const INPUT_CONTEXT_SUMMARY_TARGET_MAX_CHARS = 300;
