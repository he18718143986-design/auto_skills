{
  "id": "wf_fixture_fail_constraint",
  "version": "2.0",
  "meta": {
    "title": "fixture fail constraint",
    "taskType": "software",
    "userInput": "fixture",
    "createdAt": "2026-05-08T00:00:00.000Z"
  },
  "stages": [
    {
      "id": "stage_decide_worker",
      "title": "decide worker",
      "tool": "llm-text",
      "toolConfig": { "type": "llm-text", "systemPrompt": "decision" },
      "input": {
        "sources": [{ "type": "user-input", "label": "需求" }],
        "mergeStrategy": "concat"
      },
      "outputs": [{ "key": "decisionRecord", "format": "markdown" }],
      "pauseAfter": true,
      "isDecisionStage": true
    },
    {
      "id": "stage_impl_worker",
      "title": "impl worker",
      "tool": "llm-text",
      "toolConfig": { "type": "llm-text", "systemPrompt": "implement with own judgment" },
      "input": {
        "sources": [
          {
            "type": "stage-output",
            "stageId": "stage_decide_worker",
            "outputKey": "decisionRecord",
            "label": "已确认的决策清单"
          }
        ],
        "mergeStrategy": "concat"
      },
      "outputs": [{ "key": "code", "format": "text" }],
      "pauseAfter": false
    }
  ]
}
