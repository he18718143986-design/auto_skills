#!/usr/bin/env node
/**
 * Legacy check retired: webview runtime source of truth is `src/webview/runtime/*.ts`
 * built via `scripts/build-webview.mjs`. `_extracted-bootstrap.js` was removed.
 */
process.exit(0);
