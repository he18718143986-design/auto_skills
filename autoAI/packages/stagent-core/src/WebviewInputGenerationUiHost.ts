import { INPUT_PAGE_BUSY_TITLES } from './WebviewInputGenerationUi';

/** Host: busy titles for postMessage progress (autoAI uses plain strings, not l10n keys). */
export const HOST_INPUT_PAGE_BUSY_TITLES = INPUT_PAGE_BUSY_TITLES;
